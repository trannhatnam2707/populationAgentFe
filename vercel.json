{
    "buildCommand": "cd agent && npm install && npm run build",
    "outputDirectory": "agent/build",
    "installCommand": "cd agent && npm install",
    "devCommand": "cd agent && npm start",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ]
  }